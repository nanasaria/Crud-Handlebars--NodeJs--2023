    <div class="navbar-auto">
      <a href="/"><button class="btn-cadastrar">Cadastrar</button></a>
    </div>
 
    <nav class="navbar navbar-light">
      <div class="container-fluid">
        <form class="d-flex">
          <input class="form-control me-2" type="search" placeholder="Pesquisar" aria-label="Search">
          <button class="button" type="submit">Search</button>
        </form>
      </div>
    </nav>

    <div class="container">
          <table class="table table-striped table-hover">
            <thead div="table">
              <tr>
                <th scope="col">ID</th>
                <th scope="col">Nome</th>
                <th scope="col">CEP</th>
                <th scope="col">Endereço</th>
                <th scope="col">Cidade</th>
                <th scope="col">Estado</th>
                <th scope="col">Bairro</th>
                <th scope="col"></th>
                <th scope="col"></th>
                </tr>
            </thead>
            <tbody>
              {{#each post}}
                {{#dataValues}}
              <tr>
                <th scope="row"></th>
                <td>{{nome}}</td>
                <td>{{cep}}</td>
                <td>{{endereco}}</td>
                <td>{{cidade}}</td>
                <td>{{estado}}</td>
                <td>{{bairro}}</td>

                <div class="button-crud">
                <td><a href="excluir.html"><button class="button-delete"><i class="bi bi-trash-fill"></button></a></td>
                <td><a href="editar.html"><button class="button-edit"> <i class="bi bi-pencil-square"></i></button></a></td>
                </div>
                    
              </tr>
              {{/dataValues}}
              {{/each}}
            </tbody>
          </table>

     
      </ul>
    </div>

    <nav class="pagination" aria-label="...">
        <ul class="pagination">
          <li class="page-item disabled">
            <span class="page-link">Previous</span>
          </li>
          <li class="page-item" ><a class="page-link" href="#">1</a></li>
          <li class="page-item active" aria-current="page">
            <span class="page-link">2</span>
          </li>
          <li class="page-item"><a class="page-link" href="#">3</a></li>
          <li class="page-item">
            <a class="page-link" href="#">Next</a>
          </li>
        </ul>
      </nav>